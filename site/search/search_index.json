{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"GeoFlood CI/CD Platform","text":"<p>GeoFlood is a compact, production-style geospatial processing platform. It demonstrates how a transparent scientific calculation can be packaged, validated, tested, containerized, scanned, and delivered as reproducible artifacts.</p>"},{"location":"#featured-study-area","title":"Featured study area","text":"<p>The portfolio demo uses a real USGS 3DEP bare-earth DEM covering the Elizabeth River and Norfolk, Virginia. The clipped area spans approximately 8 by 9 kilometers and includes tidal shoreline, river branches, and low-relief urban terrain. The download script records its source URL, extent, CRS, access date, and elevation range in a JSON provenance file.</p> <p>Synthetic data are retained for automated tests and CI reliability.</p> <p>The Norfolk source raster includes negative coastal/water elevations. GeoFlood therefore emits visible warnings when modeled depth exceeds the scenario water level. See data quality and interpretation for vertical datum and land-mask guidance.</p>"},{"location":"#processing-model","title":"Processing model","text":"<p>For each valid DEM cell:</p> <pre><code>flood_depth = max(water_level - terrain_elevation, 0)\n</code></pre> <p>The output retains the source grid, coordinate reference system, affine transform, and nodata convention. A positive value means the constant water-surface elevation is above the terrain cell.</p> <p>Model scope</p> <p>This is a static inundation screening model. It does not model flow connectivity, rainfall-runoff, velocity, levee failure, uncertainty, or real-time conditions and must not be treated as an operational forecast. Water levels and DEM elevations must also use compatible vertical datums.</p>"},{"location":"#interfaces","title":"Interfaces","text":"<ul> <li><code>geoflood run</code> supports repeatable local and batch processing.</li> <li><code>POST /run-flood-scenario</code> supports path-based processing in a service or   mounted-container environment.</li> <li>Optional zone polygons produce CSV summaries suitable for reporting.</li> </ul> <p>Start with the architecture, then see the CI/CD design and API reference.</p>"},{"location":"api/","title":"API","text":"<p>Run locally with:</p> <pre><code>uvicorn geoflood.api:app --reload\n</code></pre> <p>FastAPI publishes interactive OpenAPI documentation at <code>/docs</code> and the schema at <code>/openapi.json</code>.</p>"},{"location":"api/#get-health","title":"<code>GET /health</code>","text":"<p>Returns service status and package version.</p> <pre><code>{\"status\": \"healthy\", \"version\": \"0.1.0\"}\n</code></pre>"},{"location":"api/#post-run-flood-scenario","title":"<code>POST /run-flood-scenario</code>","text":"<p>Request:</p> <pre><code>{\n  \"dem_path\": \"tests/data/sample_dem.tif\",\n  \"water_level\": 3.2,\n  \"output_name\": \"api_depth.tif\"\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"output_path\": \"C:/project/outputs/api_depth.tif\",\n  \"water_level\": 3.2,\n  \"valid_cell_count\": 99,\n  \"flooded_cell_count\": 52,\n  \"max_depth\": 3.139394,\n  \"mean_depth\": 0.842118,\n  \"warnings\": []\n}\n</code></pre> <p>The path must be visible inside the API process. With Docker Compose, reference the sample input as <code>/data/sample_dem.tif</code>. Generated files are placed in <code>GEOFLOOD_OUTPUT_DIR</code>, which defaults to <code>outputs</code>.</p>"},{"location":"api/#status-codes","title":"Status codes","text":"Code Meaning <code>200</code> Scenario completed <code>404</code> DEM path was not found <code>422</code> Request validation or raster processing failed"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#component-flow","title":"Component flow","text":"<pre><code>flowchart TB\n    subgraph Interfaces\n        CLI[\"Typer CLI\"]\n        API[\"FastAPI + Pydantic\"]\n    end\n    subgraph Domain\n        Model[\"Flood-depth calculation\"]\n        Service[\"Scenario service\"]\n        Zonal[\"Zonal statistics\"]\n    end\n    subgraph GeospatialIO[\"Geospatial I/O\"]\n        Reader[\"Rasterio reader\"]\n        Writer[\"Rasterio GeoTIFF writer\"]\n        Vector[\"GeoPandas zone reader\"]\n    end\n\n    CLI --&gt; Service\n    API --&gt; Service\n    Reader --&gt; Service\n    Service --&gt; Model\n    Model --&gt; Writer\n    Writer --&gt; Zonal\n    Vector --&gt; Zonal</code></pre> <p>Both external interfaces delegate to <code>run_flood_scenario</code>, preventing the CLI and API from developing different scientific behavior.</p>"},{"location":"architecture/#data-contracts","title":"Data contracts","text":"<p>The raster layer accepts one single-band DEM. The output uses <code>float32</code>, DEFLATE compression, and the source nodata value. The API validates water-level bounds, GeoTIFF extensions, and output filenames before disk access.</p>"},{"location":"architecture/#operational-boundaries","title":"Operational boundaries","text":"<ul> <li>A configurable cell-count limit protects the API from unexpectedly large   requests.</li> <li>Output filenames cannot contain path separators.</li> <li>The Docker image runs as an unprivileged user.</li> <li>Automated test datasets are deterministic and synthetic.</li> <li>The real Norfolk demo is downloaded reproducibly from USGS 3DEP and includes   machine-readable provenance; CI tests do not depend on that network request.</li> <li>Scenario reports preserve warnings for negative elevations, depth greater   than water level, missing CRS, and missing nodata metadata.</li> <li>The platform is path-based by design; object storage and job queues are   plausible future extensions, not hidden dependencies.</li> </ul>"},{"location":"ci_cd/","title":"GitLab CI/CD","text":"<p>The pipeline emphasizes early feedback and immutable evidence.</p> <pre><code>flowchart LR\n    V[\"validate\"] --&gt; T[\"test\"]\n    T --&gt; B[\"build\"]\n    B --&gt; S[\"scan\"]\n    S --&gt; P[\"package\"]\n    P --&gt; D[\"docs\"]</code></pre>"},{"location":"ci_cd/#stages","title":"Stages","text":"<ol> <li>Validate runs Ruff, Black, and strict mypy.</li> <li>Test regenerates synthetic inputs and publishes coverage.</li> <li>Build creates a wheel/source distribution and pushes a commit-tagged    image to the GitLab Container Registry.</li> <li>Scan audits Python dependencies and scans the pushed image with Trivy.</li> <li>Package runs a representative flood scenario and retains GeoTIFF, CSV,    and JSON outputs as expiring artifacts.</li> <li>Docs builds strict MkDocs content; the default branch publishes the    <code>public/</code> artifact for GitLab Pages.</li> </ol>"},{"location":"ci_cd/#reproducibility","title":"Reproducibility","text":"<p>The demo job generates its own small inputs. CI does not download operational flood data, depend on private file shares, or write outside the project workspace. Package and output artifacts expire after one week to control storage.</p>"},{"location":"ci_cd/#security-posture","title":"Security posture","text":"<p>The application image runs as a non-root user and includes a health check. <code>pip-audit</code> is advisory so newly disclosed dependency findings remain visible without abruptly blocking development. Trivy fails the pipeline for critical container findings.</p>"},{"location":"data_quality/","title":"Data quality and interpretation","text":""},{"location":"data_quality/#norfolk-case-study","title":"Norfolk case study","text":"<p>The real case study covers the tidal Elizabeth River and Norfolk, Virginia. Its bare-earth DEM is downloaded from the USGS 3D Elevation Program and clipped to a small UTM Zone 18N grid. Source URL, access date, bounds, CRS, and observed elevation range are retained in <code>data/norfolk_elizabeth_river_dem.json</code>. The current source record does not establish a vertical datum, so reports keep <code>vertical_datum</code> as <code>null</code> rather than inferring one.</p> <p>Synthetic rasters remain the basis of automated tests so CI does not depend on external data availability.</p>"},{"location":"data_quality/#bathtub-model-limitations","title":"Bathtub-model limitations","text":"<p>GeoFlood calculates:</p> <pre><code>depth = max(water_surface_elevation - terrain_elevation, 0)\n</code></pre> <p>This simplified bathtub method does not test whether a low cell is connected to the coast or river. It also does not model flow, rainfall-runoff, drainage, levees, velocity, or time.</p>"},{"location":"data_quality/#vertical-datum-compatibility","title":"Vertical datum compatibility","text":"<p>The water-surface elevation and DEM must share vertical units and a compatible vertical datum. A <code>2 m</code> water level referenced to one datum cannot be safely subtracted from terrain referenced to another. Horizontal CRS metadata does not by itself prove that vertical datums match.</p>"},{"location":"data_quality/#negative-elevations-and-water-cells","title":"Negative elevations and water cells","text":"<p>The Norfolk subset contains elevations below <code>-1 m</code>. These may represent valid low terrain, interpolation over water, bathymetry, or datum effects. Because the calculation is subtraction, a DEM value of <code>-7.21 m</code> produces a depth near <code>9.21 m</code> under a <code>2 m</code> water surface.</p> <p>GeoFlood reports warnings when:</p> <ul> <li>valid DEM elevations fall below <code>-1 m</code>;</li> <li>maximum depth exceeds the scenario water level;</li> <li>CRS metadata are missing;</li> <li>nodata metadata are missing.</li> </ul> <p>Warnings are emitted in both CLI output and the JSON report.</p>"},{"location":"data_quality/#report-provenance-and-derived-quantities","title":"Report provenance and derived quantities","text":"<p>When a same-stem provenance JSON exists beside a DEM, GeoFlood includes its location name, source, source URL, download date, and vertical datum in the scenario report. A different provenance file can be supplied with <code>--provenance</code>.</p> <p>Reports include projected bounds, WGS 84 longitude/latitude bounds, total area, flooded area in square meters, square kilometers, and acres, plus estimated flood volume:</p> <pre><code>estimated_flood_volume_m3 =\n    mean_depth_flooded_cells_m * flooded_area_m2\n</code></pre> <p>This is a geometric raster summary for a simplified bathtub scenario. It is not a hydrodynamic storage, flow, or discharge calculation.</p>"},{"location":"data_quality/#land-mask-workflow","title":"Land-mask workflow","text":"<p>For a land-only screening product, preprocess the DEM using an authoritative, reviewed land polygon:</p> <ol> <li>Reproject the polygon to the DEM CRS.</li> <li>Raster-mask cells outside the land polygon to the DEM nodata value.</li> <li>Inspect shoreline alignment at the raster resolution.</li> <li>Run GeoFlood on the masked DEM.</li> <li>Preserve the mask source, date, CRS, and processing command as provenance.</li> </ol> <p>Do not automatically discard all negative terrain: legitimate subsided or below-sea-level land exists. Masking should be based on a land/water dataset, not an elevation threshold alone.</p>"}]}